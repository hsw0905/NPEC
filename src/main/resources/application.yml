spring:
  profiles:
    active: local

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/npec
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

  config:
    import: secret.yml

  cache:
    type: redis

---
spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:h2:~/npec

  jpa:
    hibernate:
      ddl-auto: update
---
spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: jdbc:h2:~/npec

  jpa:
    hibernate:
      ddl-auto: create
---
spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:h2:~/npec

  jpa:
    hibernate:
      ddl-auto: create
